# Base requirements (required for all providers)
-r requirements-base.txt

# Optional: Install all provider dependencies
# Uncomment the line below or install with: pip install -e .[all]
# -r requirements-providers.txt

# To install specific providers:
# pip install -e .[bedrock]  # For AWS Bedrock
# pip install -e .[search]   # For web search feature
# pip install -e .[all]      # For all optional features 